{
  "name": "ga-proxy",
  "version": "1.0.0",
  "description": "Simple proxy to forward requests to Google Apps Script and handle CORS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.9.0",
    "cors": "^2.8.5"
  }
}

